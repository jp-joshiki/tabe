# Dev env

Requirents: Docker, Python3.7, Pipenv

```shell
$ pipenv install
$ pipenv shell
$ python manage.py db upgrade
$ python manage.py update_db
```

# Run spiders

```shell
python -m spiders
```
